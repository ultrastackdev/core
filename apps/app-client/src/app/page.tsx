import { NewUltrastackFrontendShared } from '@shared/front/lib/frontend-shared';

export default function Index() {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./index.tailwind file.
   */
  return (
    <div>
      hello from app-client
      <NewUltrastackFrontendShared />
    </div>
  );
}
